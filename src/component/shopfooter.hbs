<div class="ps-5">
    <hr>
  </div>

  {{#with footer}}
  <footer class="row p-5">
    <div class="col-3">
      {{#each [0]}}

      <div class="nav my-3">
        <div class="nav nav-item">
          <a href="{{link}}" class="nav-link {{#if @last}} disabled {{/if}}"> {{text}}
          </a>
        </div>

      </div>
      {{/each}}

    </div>
    <div class="col-3">
      {{#each [1]}}

      <div class="nav my-3">
        <div class="nav nav-item">
           <a href="{{link}}" class="nav-link {{#if @last}} disabled {{/if}}"> {{text}}
          </a>
        </div>

      </div>
      {{/each}}

    </div>
    <div class="col-3">
      {{#each [2]}}

      <div class="nav my-3">
        <div class="nav nav-item">
           <a href="{{link}}" class="nav-link {{#if @last}} disabled {{/if}}"> {{text}}
          </a>
        </div>

      </div>
      {{/each}}

    </div>
    <div class="col-3">
      {{#each [3]}}

      <div class="nav my-3">
        <div class="nav nav-item">
          <a href="{{link}}" class="nav-link {{#if @last}} disabled {{/if}}"> {{text}}
          </a>
        </div>

      </div>
      {{/each}}

    </div>
  </footer>
  {{/with}}